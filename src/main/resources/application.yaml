server:
  port: 8081
spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=clinic_appointment_db;encrypt=true;trustServerCertificate=true
    username: sa
    password: taomatkhauL7
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect 
        format_sql: true